# 音節劃分規則

## 一、 核心原則

* **單詞組成**：單詞可由一個或多個合法音節組成。
* **集合基礎**：合法音節構成一個有限集合，具體列表參見《7 溜歌粵語合法音節集合》。**合法音節僅由 26 個英文字母組成**，不包含任何標點或特殊符號。
* **消歧義策略**：當一個單詞存在多種合法切分方式時，遵循「優先選取字符長度最大的右端合法音節」原則進行優先匹配。

## 二、 算法推導機制

切分過程採用由右至左的**逆向字符長度最大匹配法**（Backward Maximum Matching）：

1. **遞歸執行**：每一步優先選取當前字串字符長度最大的右端合法音節進行切分。
2. **符號處理**：若在逆向檢索過程中遇到非 26 個字母的非法符號（如 `-`、`'` 等），則將其**直接去除**，並以該符號位置作為天然邊界，繼續向左遞歸切分。
3. **終止條件**：切出右端音節後，再對左側剩餘部分遞歸執行相同規則，直至完成整個單詞的切分。

---

## 三、 實例演示

### 實例 1：單一合規切分（無歧義）

* **單詞**：`hamhbaanghlaangh`
* **切分結果**：`hamh · baangh · laangh`
* **說明**：該詞自右向左檢索時，依序只能匹配出唯一的合法音節 `laangh`、`baangh`、`hamh`，直接完成劃分。

### 實例 2：具歧義的多重切分（策略應用）

* **單詞**：`saataa`
* **潛在切分路徑**：
* **路徑 A**：`saa · taa`
* **路徑 B**：`saat · aa`


* **「右端合法音節字符長度最大」逆向推導流程**：
1. **第一步（自右向左檢索）**：從最右端開始考慮，比對合法音節集合。此時右端可匹配出的候選合法音節有 `aa`（路徑 B，字符長度 2）與 `taa`（路徑 A，字符長度 3）。
2. **第二步（最大化篩選）**：比較字符長度，`taa`（3 個字母）大於 `aa`（2 個字母）。根據「優先選取字符長度最大的右端合法音節」原則，**優先選取較長音節 `taa**`。
3. **第三步（剩餘部分切分）**：切出 `taa` 後，左側剩餘部分為 `saa`。經檢索 `saa` 亦為合法音節，切分結束。


* **最終默認劃分結果**：`saa · taa`

### 實例 3：包含特殊符號「'」的情況

* **單詞**：`hor'`
* **切分結果**：`hor`
* **說明**：自右向左檢索時，結尾符號 `'` 不在 26 個合法字母集合內，判定為非法符號，直接去除。隨後得到合法音節 `hor`，切分結束。

### 實例 4：包含連字號「-」的情況

* **單詞**：`zih-oi`
* **切分結果**：`zih · oi`
* **說明**：自右向左檢索，首先得到最右端的合法音節 `oi`；接著向左遇到非法的連字號 `-`，直接將其去除；隨後繼續向左檢索，得到合法音節 `zih`，切分結束。

### 實例 5：多符號與多音節混合的複雜情況

* **單詞**：`hamh-neiq-go-gaajcaanr`
* **切分結果**：`hamh · neiq · go · gaaj · caanr`
* **逆向推導完整路徑**：
1. 從最右端開始，依「字符長度最大」原則，先後成功切分出合法音節 `caanr` 與 `gaaj`。
2. 繼續向左檢索，遇到非法符號 `-`，直接去除。
3. 繼續向左，切分出合法音節 `go`。
4. 再次遇到非法符號 `-`，直接去除。
5. 繼續向左，切分出合法音節 `neiq`。
6. 再次遇到非法符號 `-`，直接去除。
7. 最後向左檢索，得到合法音節 `hamh`，全詞切分結束。



## 四、 

若剩餘字符串無法匹配任何合法音節：

則：

判定為非法單詞。