溜歌粵語正字與音節劃分規則

一、 音節規範 (Syllable Specification)
音節按其在語言實踐中的有效性，分為合法音節（Valid Syllable）與非法音節（Invalid Syllable）。

1.1 非法音節：在當前語言實踐或語料庫中不被採納、不使用的音節。

1.2 合法音節：允許在系統中使用的有效音節，其構成一個有限集合（具體參見《7 溜歌粵語合法音節集合》）。合法音節僅由 26 個英文字母組成，不包含任何標點或特殊符號。合法音節進一步細分為以下兩種：

可組合音節 (Combinable Syllable)：可單獨成詞，或與其他「可組合音節」組合構成多音節詞。

不可組合音節 (Non-combinable Syllable)：只可單獨成詞（如特定口語詞、感嘆詞），不可與任何合法音節組合成多音節詞。

二、 單詞規範 (Word Specification)
單詞按其組成音節的屬性，分為合法單詞（Valid Word）與非法單詞（Invalid Word）。

非法單詞包括但不限於：

2.1 含有任何「非法音節」的單詞。

2.2 含有「不可組合音節」的多音節單詞。

合法單詞包括以下兩種：

3.1 由任意合法音節（可組合或不可組合）單獨組成的單音節單詞。

3.2 完全由「可組合音節」串聯組成的多音節單詞。
【連字號使用準則】：系統追求極致純淨，不論音節長短（包含四音節及以上之長詞），預設一律不強制加入連字號 -。只有當該詞在純字母狀態下會導致切分錯誤，且無法透過「一步回溯」自動修復時，才需人工加入 - 進行邊界鎖定。


三、 演算法推導機制：具有限回溯的逆向最長匹配規則 (RLMM with Bounded Backtracking)
當一個輸入字串需要進行音節切分時，系統採用由右至左的逆向最長匹配規則（RLMM），並引入一步回溯機制以修正貪婪誤差：

[!IMPORTANT]
升級版 RLMM 規則（次長匹配備援與算力熔斷）

優先最長匹配：在自右向左切分過程中，若存在多個右端合法音節候選，系統優先選取字符長度最長者。

合規性前瞻（Lookahead）：切出最長右端音節後，若發現左側剩餘字串無法匹配任何合法音節（即變為非法音節），系統將拒絕當前的最長匹配，並回溯一步，改為選取「次長」的右端合法音節。

算力熔斷條款（Computational Cut-off）：為極大化節省算力，本演算法拒絕多步回溯。若一個單詞在切出最長音節後，左側剩餘部分仍可被 RLMM繼續完全切分為合法音節序列、但屬於錯誤切分（導致連鎖錯誤，需要兩步以上的深層回溯才能導正），此情況直接觸發熔斷。系統不進行深層計算，認定該單詞非法。

詳細執行步驟：符號處理：若在逆向檢索過程中遇到非 26 個字母的非法符號（如 -、' 等），則將其直接去除/忽略，並以該符號位置作為天然邊界，繼續向左遞歸切分。長度篩選與回溯：嘗試切出最長右端音節 $S_{max}$。檢查左側剩餘部分：若左側剩餘部分合法 $\rightarrow$ 成功切分，繼續向左遞歸（若此處隱藏連鎖錯誤，將在後續觸發異常或由 - 防禦）。若左側剩餘部分變成非法 $\rightarrow$ 觸發一步回溯，放棄 $S_{max}$，改切次長右端音節 $S_{sub\_max}$。終止條件：完成整個單詞的切分。


四、 實例演示 (Examples)
實例 1：單一合規切分（無歧義多音節詞）
單詞：hamhbaanghlaangh

切分結果：hamh · baangh · laangh

屬性判定：所有切出音節均為「可組合音節」，判定為 【合法多音節單詞】。

實例 2：具歧義的多重切分（RLMM 仲裁）
單詞：saataa

潛在路徑：路徑 A (saa · taa) / 路徑 B (saat · aa)

RLMM 推導：自右向左檢索時，右端候選有 aa（長度 2）與 taa（長度 3）。觸發 RLMM，強制選取較長之 taa。左側剩餘 saa 亦合規。

最終結果：saa · taa

屬性判定：判定為 【合法多音節單詞】。

實例 3：包含特殊符號「 ' 」的情況
單詞：hor'

切分結果：hor

說明：自右向左檢索時，結尾符號 ' 不在 26 個合法字母集合內，判定為非法符號，直接去除。隨後得到合法音節 hor，切分結束。

實例 4：包含特殊符號「 ' 」與「 - 」
單詞：zih-oi

切分結果：zih · oi

說明：逆向檢索時忽略中間的 -，正常切出 oi 與 zih。

屬性判定：判定為 【合法多音節單詞】。



五、 異常與合規性檢查 (Exception & Validity Handling)
在完成 RLMM 切分後，系統必須依據「第一章」與「第二章」的規範進行最終合規性檢查：

[!CAUTION]
非粵語單詞（非法單詞）判定

任何輸入單詞若觸發以下任一條件，系統將中止程序並判定為非粵語單詞：

切分失敗：若剩餘字串無法匹配任何《7 溜歌粵語合法音節集合》中的音節（即含有「非法音節」）。

結構違規（觸發結構約束）：若單詞成功切分出多個音節，但其中包含一個或多個「不可組合音節 (Non-combinable Syllable)」（違反單詞規範 2.2）。