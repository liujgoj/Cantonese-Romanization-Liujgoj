

# 溜歌粵語拼音單詞拼寫規則
## （由Geminin3 協助編寫）

## 第一部分 制定原則

1. **基礎依據**：本規則係喺《溜歌粵語拼音方案》確定嘅音節拼寫規則基礎之上，進一步規定單詞嘅拼寫規則。
2. **書寫單位**：以單詞為拼寫單位。

---

## 第二部分 總則

1. **基本單位**：拼寫粵語基本上以單詞為書寫單位。
* 例如：`yanx` (人)、`lengzair` (靚仔)、`sihtauxpox` (事頭婆)。


2. **重疊式**：
* 單音節重疊連寫，如 `ninxninx` (年年)。
* 雙音節重疊分寫，如 `yinxgau yinxgau` (研究研究)。
* AABB 式結構連寫，如 `cingjcingjcorcor` (清清楚楚)。


3. **構詞成分**：前綴（副、總、非等）或後綴（者、員、家、部等）與主體連寫。
* 例如：`fubouhzoengr` (副部長)、`ngaihseothgaaj` (藝術家)、`wuihgaibouh` (會計部)。


4. **連接號使用**：並列語素、縮略語或約數之間可用連接號“-”。
* 例如：`baat-gaur yath` (八九日)、`Gwongr-Zvj sin`(廣珠線) 、`saphcatj-baat seoi`(十七八歲)


5. **單詞插入**：
* 插入兩個單詞之間，前後加空格：`Hour gwair faanx` (好鬼煩)、`mx gwair mingx` (唔鬼明)。
* 插入單詞中間，前後加連接號：`maax-gwair-faanx` (麻鬼煩)。


6. **省略標記**：被省略嘅部分用“ ' ”表示。
* 例如：`mingx' mx mingxbaakh` (明唔明白)。



---

## 第三部分 細則

### 1. 名詞與方位詞

* 名詞與方位詞分寫，如 `saanj soengh` (山上)。
* 已經成詞（固定搭配）者連寫，如 `tinjsoengh` (天上)、`hoirngoih` (海外)。
* 詞綴性方位語素（binh, minh）連寫，如 `zorbinh` (左便)、`yauhminh` (右面)。

### 2. 人名拼寫邏輯（統一修訂版）

為統一邏輯，凡涉及人名與其職位、稱呼之組合，採取**「專名大寫、功能名小寫」**原則：

* **基本姓名**：姓與名分寫，首字母大寫；複姓連寫；雙姓連寫，首字母大寫，兩個姓嘅首字母都大寫。
* 例如：`Leiq Waax` (李華)、`Dungjfongj Sok` (東方朔)、`ZoengjWongx Sukhfanj` (张王淑芳)。


* **人名 + 稱謂/職位**：一律分寫，職稱小寫。
* 例如：`Wongx bouhzoengr` (王部長)、`Leiq saangj` (李生)、`Faat goj` (發哥)、`Zoengj zungr` (張總)、`Zoengj soex` (張Sir)。


* **前置修飾 + 人名**：分寫，修飾詞（阿、老、小等）小寫。
* 例如：`aa Lokh` (阿樂)、`louq Cinx` (老錢)、`aa Leiq saangj` (阿李生)、`aa Haax yij` (阿霞姨)。


* **英文組合**：英文部分首字母大寫，分寫。
* 例如：`Sunny zair` (Sunny仔)。


* **親屬/普通稱謂**：連寫，首字母小寫。
* 例如：`aagoj` (阿哥)、`aayij` (阿姨)、`aabaaj` (阿爸)、`aasoex` (阿Sir)`louqbaak` (老伯)。


* **特例**：
* 已專名化（歷史人物或組織）連寫：`Kungrzir` (孔子)、`Baaujgungj` (包公)。
* 常用代詞連寫：`aabinjgo` (邊個)。
* 喺粤语中，“记”係一个后缀性词素（Suffix），它唔係一个独立嘅单词（Independent Word），而係一个专门用于构成店名或昵称嘅语言单位。
* 例如：`Oujgei` (O記)，`Sanjgei` (新記)。



### 3. 動詞與態

* 動詞與態後綴（緊、咗）連寫。
* 例如：`sikhganr faanh` (食緊飯)、`sikhzor faanh` (食咗飯)。



### 4. 數詞、量詞與編號

* **位數**：十、百、千、萬、億與數字分寫。
* **序數**：全部分寫（`daih yatj`（第一）、`daih yatj go`（第一個））；大數字建議用阿拉伯數字（`daih 118`）。
* **日期**：月日中間加連接號。ngq-sei wanhdungh(五四運動)；yatjyih-gaur sihginr （一二·九事件）。
* **編號**：字母數字連寫（`AK47`）；縮寫與單詞分寫（`CID 9527`）。
* **约数**：两个邻近嘅数字或表位数嘅单位并列表示约数, 中间加连接号“-”。例如: `saamj-ngq yath (三五日)`、 catj-baat go(七八个)。

### 5. 成語與熟語 (Idioms and Idiomatic Expressions)

成語與熟語（包括四字格、固定慣用語等）處於「詞」與「短語」嘅邊界。為平衡視覺掃描效率、語流韻律以及計算機模型嘅處理性能，特制定以下切分與標記規則：

* **5.1 完全詞化疊詞**

凡屬於完全詞化（Fully Lexicalized）嘅重疊形式，一律採用完全連寫形式。

規則說明：此類疊詞已喺語意上高度融合為單一概念焦點，散寫會割裂其語意完整性。

範例：

sijsijmanxmanx (斯斯文文)

daaihdaaihsengjsengj (大大聲聲)

* **5.2 並列聯合結構**

凡四字格成語或熟語，其內部由兩個語法地位平等嘅語塊構成，並共同形成固定整體意義，使用「2+2 黐埋 + 中間加連字號（-）」作為標準詞組標記。

範例：

mxsaamj-mxsei (唔三唔四)

mouqzing-mouqgeoi (冇證冇據)

yoengxmingx-laphmaanh (揚名立萬)

設計目嘅與理論支撐：

* 5.2.1 認知與視覺辨識：
既可以維持四字格嘅整體邊界（Gestalt Boundary），又可以減輕視覺上因字母過長而產生嘅掃描辨識壓力（Eye-movement Overload）。

* 5.2.2 語音與韻律流動：
中間嘅連字號（-）扮演「語流緩衝」與「結構標記」嘅雙重角色，精確噉對齊人類喺朗讀或說出該成語時，內心深處嘅語意停頓同節奏切分（2+2 抑揚節奏）。

* 5.2.3 計算機狀態機最優化：
可以令語言模型（Language Models）喺進行狀態轉移預測（State Transition Prediction）時，將前半部分（如 yoengxmingx）與後半部分（如 laphmaanh）作為兩個高頻嘅「半開放節點（Semi-open Nodes）」儲存。
噉既避免咗詞表（Vocabulary）因塞入大量「四字盲盒 Token」而產生維度爆炸（Dimensionality Explosion），又通過連字號（-）標記，強制模型鎖定後半部分嘅並列組合關係，在 $O(1)$ 的符號匹配（Symbolic Matching）中達到最高嘅訊息密度（Information Density）。

* **5.3 非並列結構**

凡唔屬於 2+2 平行並列聯合結構嘅四字格、固定熟語或短語，一律按單詞分寫。

規則說明：此類結構多屬於偏正、動賓、動補或主謂結構，其內部語意黏合度未達完全並列鎖定狀態，分寫更符合口語流動與單詞識別。

範例：

gongr hoij yauh gongr (講開又講)

yatj yijdaai seoir (一衣帶水)

pungxbatj sangjfaij (蓬蓽生輝)


---



## 第四部分 輔助規則

### 1. 大寫規則

* 句子開頭、專有名詞首字母大寫。
* 專有名詞首字母大寫（`Yvthyvq` 粵語）；
* 普通名詞首字母小寫（`yvthkekh` 粵劇）；
* 「地名 + 人」原則上分寫: 地名首字母大寫，“人”小寫（`Gwongrdungj yanx` 廣東人）。只有喺刻意身份化、標籤化嘅語境，先允許合寫，且不視為詞綴派生。


### 2. 縮寫規則

* 取每個拼寫單位或音節嘅首字母，大寫連寫。
* 例如：`Gwongrdungj` (GD)。


* 粵語人名原則上不縮寫。

### 3. 移行規則

* 移行按單詞分開，單詞內部及縮寫詞不可拆分。

---

**參考資料**：《漢語拼音正詞法基本規則》

